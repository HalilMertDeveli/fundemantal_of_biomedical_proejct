import 'package:flutter/material.dart';

class PageBasicLogin extends StatelessWidget {
  const PageBasicLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Basic Login Page"),
      ),
      body: Column(
        children: [
          
        ],
      ),
    );
  }
}